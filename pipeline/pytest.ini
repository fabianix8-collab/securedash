[pytest]
# Permite "from detection_engine import ..." y "from tests.conftest import ..."
# desde dentro de pipeline/, sin necesidad de instalar el proyecto como paquete.
pythonpath = .
testpaths = tests
